<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="booksys.persistency.TableMapper">
	<select id="findList" resultType="Table">
		SELECT
			oid,
			number,
			places
		FROM Table
	</select>
	
	<select id="findOneByCode" parameterType="int" resultType="Table">
		SELECT
			oid,
			number,
			places
		FROM `Table`
		WHERE number=#{number}
	</select>
	
	<insert id="save" parameterType="Table">
		INSERT INTO Table(
			number,
			places		
		) VALUES(
			#{number},
			#{places}
		)
	</insert>
	
	<update id="update" parameterType="Table">
		UPDATE Table
		SET
			number=#{number},
			places=#{places}
		WHERE oid=#{oid}
	</update>
	
	<delete id="deleteById" parameterType="int">
		DELETE FROM Table WHERE oid=#{oid}
	</delete>
</mapper>